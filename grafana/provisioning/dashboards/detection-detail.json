{
  "id": null,
  "uid": "detection-detail-dashboard",
  "title": "Cloud Anomaly Detection - 상세 지표",
  "timezone": "browser",
  "editable": true,
  "schemaVersion": 39,
  "version": 6,
  "refresh": "30s",
  "time": { "from": "now-7d", "to": "now" },
  "links": [
    {
      "title": "메인 대시보드로 돌아가기",
      "type": "link",
      "url": "/d/detection-dashboard/",
      "icon": "external link",
      "targetBlank": false
    }
  ],
  "panels": [
    {
      "id": 5,
      "type": "timeseries",
      "title": "시간대별 이상 탐지 추이",
      "gridPos": { "x": 0, "y": 0, "w": 24, "h": 8 },
      "datasource": { "type": "postgres", "uid": "postgresql" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgresql" },
          "format": "time_series",
          "rawSql": "SELECT\n    DATE_TRUNC('hour', finished_at) AS \"time\",\n    COUNT(*) AS \"탐지 건수\"\nFROM agent_runs\nWHERE anomaly_flag = true\n  AND $__timeFilter(finished_at)\nGROUP BY DATE_TRUNC('hour', finished_at)\nORDER BY 1;",
          "refId": "A"
        }
      ],
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "options": {}
    },
    {
      "id": 9,
      "type": "barchart",
      "title": "시나리오별 비용 절감액",
      "description": "메인 대시보드 '비용 절감액'과 동일한 방식(시간당 절감액 × 지속시간)을 이상 유형/조치별로 나눠서 집계 — 전부 더하면 메인의 총액과 일치함",
      "gridPos": { "x": 0, "y": 8, "w": 24, "h": 8 },
      "datasource": { "type": "postgres", "uid": "postgresql" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgresql" },
          "format": "table",
          "rawSql": "WITH action_durations AS (\n    SELECT\n        al.resource_id,\n        ar.anomaly_type,\n        al.action_name,\n        al.executed_at,\n        (elem->>'estimated_saving_usd')::float AS hourly_saving,\n        LEAD(al.executed_at) OVER (\n            PARTITION BY al.resource_id ORDER BY al.executed_at\n        ) AS next_action_at\n    FROM action_log al\n    JOIN agent_runs ar ON al.run_id = ar.run_id\n    JOIN agent_steps s ON s.run_id = ar.run_id AND s.step_name = 'decision'\n    CROSS JOIN LATERAL jsonb_array_elements(s.output->'candidate_actions') AS elem\n    WHERE elem->>'action' = ar.selected_action\n      AND al.success = true\n      AND $__timeFilter(al.executed_at)\n)\nSELECT\n    (anomaly_type || ' / ' || action_name) AS \"시나리오\",\n    ROUND(SUM(\n        hourly_saving * EXTRACT(EPOCH FROM (COALESCE(next_action_at, now()) - executed_at)) / 3600\n    )::numeric, 2) AS \"절감액(USD)\"\nFROM action_durations\nGROUP BY anomaly_type, action_name\nORDER BY 2 DESC;",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": { "unit": "currencyUSD" },
        "overrides": []
      },
      "options": {
        "xField": "시나리오",
        "orientation": "auto",
        "legend": { "displayMode": "list", "placement": "bottom" }
      }
    },
    {
      "id": 4,
      "type": "stat",
      "title": "조치 실패로 인한 롤백 건수",
      "description": "action_log에는 rolled_back 컬럼이 없어 action_result JSONB에서 꺼내서 집계",
      "gridPos": { "x": 0, "y": 16, "w": 8, "h": 6 },
      "datasource": { "type": "postgres", "uid": "postgresql" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgresql" },
          "format": "table",
          "rawSql": "SELECT COUNT(*) AS \"value\" FROM action_log WHERE (action_result->>'rolled_back')::boolean = true;",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "fixed", "fixedColor": "red" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "value",
        "graphMode": "none"
      }
    },
    {
      "id": 22,
      "type": "table",
      "title": "자동 조치 실패 이력",
      "description": "action_log.success=false인 건 — 실패 원인은 action_result->>'error'(실행 자체 실패) 또는 rolled_back=true(QA 실패로 롤백)에서 판단",
      "gridPos": { "x": 8, "y": 16, "w": 16, "h": 6 },
      "datasource": { "type": "postgres", "uid": "postgresql" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgresql" },
          "format": "table",
          "rawSql": "SELECT\n    ar.resource_type   AS \"리소스\",\n    ar.resource_id     AS \"ID\",\n    ar.anomaly_type    AS \"이상유형\",\n    al.action_name     AS \"액션\",\n    al.risk_level      AS \"위험도\",\n    al.executed_at     AS \"실행시각\",\n    CASE\n        WHEN (al.action_result->>'rolled_back')::boolean = true THEN 'QA 실패로 롤백됨'\n        WHEN al.action_result->>'error' IS NOT NULL THEN al.action_result->>'error'\n        ELSE COALESCE(al.action_result->>'status', '알 수 없음')\n    END AS \"실패 원인\"\nFROM action_log al\nJOIN agent_runs ar ON al.run_id = ar.run_id\nWHERE al.success = false\n  AND $__timeFilter(al.executed_at)\nORDER BY al.executed_at DESC\nLIMIT 50;",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "위험도" },
            "properties": [
              { "id": "custom.cellOptions", "value": { "type": "color-background" } },
              {
                "id": "mappings",
                "value": [
                  {
                    "type": "value",
                    "options": {
                      "LOW": { "text": "LOW", "color": "blue", "index": 0 },
                      "MED": { "text": "MED", "color": "orange", "index": 1 },
                      "HIGH": { "text": "HIGH", "color": "red", "index": 2 }
                    }
                  }
                ]
              }
            ]
          },
          {
            "matcher": { "id": "byName", "options": "실패 원인" },
            "properties": [
              { "id": "custom.cellOptions", "value": { "type": "color-text" } },
              { "id": "color", "value": { "mode": "fixed", "fixedColor": "red" } }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      }
    }
  ]
}
